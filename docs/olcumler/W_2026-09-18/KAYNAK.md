# Protokol W sonucu (2026-09-18)

`S_W.json` — `scripts/w_kiyas_raporu.py` (kod `11a8d09`, A91 desen düzeltmesinden
sonra), TRUBA `egitimg16u3`, `/arf/scratch/egitimg16u3/driftclaude/kampanya/`.
TRUBA SHA-256 (kaynak dosya, girintili biçim):
`e5470a237460f5d155115996fd5284f6241d0dda9cffd45bec201a07de0772b8`. Bu dizindeki
kopya aynı içeriğin **sıkıştırılmış yazımıdır** (alanlar ve değerler aynı).

İş `1566866_0 … _5`, hepsi `COMPLETED 0:0` (süreler 1:58 – 4:10). Kilitli
yargı **OKUNMAZ**: altı koşunun altısı yalnız `momentum_defteri` denetiminden
geçersiz (artık `2,2e-3 – 5,0e-3`, eşik `1e-3`) — sebep A92. `β` değerleri
**okunmadı** (rapor her satırı `OKUNMAZ` yazdı). Tekrar: PROTOKOL-W2.
Defter: KAYIT-066.
